# configuration file for development environment

log: "core"
logger: "console"
#log_path: "/webdocs/websites/autohome.mallard.au/log"
warnings: 1
show_errors: 1
#route_cache: 1
auto_reload: 0

plugins:
  DBIC:
    default:
      dsn: dbi:mysql:autohome:devdb
      schema_class: autohome::Schema
      user: matt
      password: notusedyet
      options:
        RaiseError: 1
        PrintError: 1

