# configuration file for production environment

log: "warning"
logger: "file"
warnings: 0
show_errors: 0
route_cache: 1


plugins:
  DBIC:
    default:
      dsn: dbi:mysql:autohome:prd002.mallard.au
      schema_class: autohome::Schema
      user: matt
      password: u95Vj8q8mtcjV54M
      options:
        RaiseError: 1
        PrintError: 1

